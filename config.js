// Edit this file to add/remove passwords.
// Put as many plaintext passwords as you want in the array below.
// NOTE: this file is served to clients, so anyone who can view it can see these values.
// For better security, use server-side auth.

const config = {
  // Add new passwords here, e.g. "pass1", "anotherPass"
  passwords: [
    "mySecret123",
    "guestPass",
    "Skolbög"
  ]
};
